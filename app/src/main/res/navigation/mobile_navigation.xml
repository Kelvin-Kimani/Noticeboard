<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.noticeboard.ui.home.HomeFragment"
        android:label="@string/posts"
        tools:layout="@layout/fragment_home"/>

    <activity
        android:id="@+id/nav_pages"
        android:name="com.noticeboard.Pages"
        android:label="@string/pages"
        tools:layout="@layout/pages"/>

    <activity
        android:id="@+id/nav_savedpost"
        android:name="com.noticeboard.SavedPost"
        android:label="@string/savedpost"
        tools:layout="@layout/activity_saved_post" />
    <activity
        android:id="@+id/nav_settings"
        android:name="com.noticeboard.UserProfile"
        android:label="@string/menu_profile"
        tools:layout="@layout/user_profile" />
</navigation>